{
  "name": "akuapem-cheque-management-system",
  "version": "1.0.0",
  "private": true,
  "description": "Akuapem Community Bank PLC cheque management demo application",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check db.js && node --check public/app.js"
  },
  "dependencies": {
    "pg": "^8.11.5",
    "@electric-sql/pglite": "^0.2.0"
  },
  "engines": { "node": ">=18" }
}
